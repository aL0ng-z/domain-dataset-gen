# 工作流修复验收记录

基于 `2026-09-12-user-workflow-review.md`，按用户确认的六批方案实施。历史数据按可重建处理；不新增导出格式或知识转问答。

| 批次 | 对应问题 | 状态与证据 |
|---|---|---|
| 1 认证权限 | R01、R02、R20 | 完成；认证状态、项目有效角色、跨项目读取及延迟响应回归通过 |
| 2 清洗编辑 | R05–R08 | 完成；租约续期、串行保存、提交前 flush、草稿保护与空正文合并回归通过 |
| 3 来源与导出 | R12、R16–R19 | 完成；分块 CAS、跨 Chunk 证据、固定来源链和导出内容门禁回归通过 |
| 4 任务恢复策略 | R09–R11、R21 后端 | 完成；迁移往返、生命周期收敛、并发策略、取消锁序和 retry 回归通过 |
| 5 解析与配置 | R03、R04、R15、R21 前端 | 完成；受控 seed、本地 MinerU、准确 DTO 表单、模板试跑和 runner 启动方式回归通过 |
| 6 批量入口与脚本 | R13、R14、Windows 门禁 | 完成；分页选择、终态新建批次、PowerShell 原生命令和 Vitest 配置回归通过 |

最终整合验收（2026-09-12）：后端 507 项测试通过、覆盖率 67.20%；前端 142 项测试、ESLint、TypeScript、API 契约检查和生产构建通过；r04 空库 upgrade→downgrade→upgrade 通过。

真实浏览器验收使用隔离 `datasetgen_e2e` 数据库、`e2e/<run-id>/` 对象前缀和本地 LLM stub：登录后完成上传两份 PDF、同文档两次解析、清洗审核、两次分块、批量生成、跨文档证据、知识审批、Dataset finalize、导出和深度验证。导出 manifest schema 为 2，保留第一份 ParseJob 与第一版 ChunkSet，导出的 QA 样本非空。测试脚本位于 `tests/e2e/`，默认不访问真实付费服务。
