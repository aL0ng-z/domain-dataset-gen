[project]
name = "cleaning"
version = "0.1.0"
description = "Section splitting and markdown cleaning utilities"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=75.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["cleaning*"]
