# AGENTS.md

## 基本规则

1. **运行环境**：代码运行使用 conda 环境 `DatasetGen`，即运行前需要先执行 `conda activate DatasetGen`。

2. **修改日志**：任何代码修改、重构都必须记录到 `DevLog.md` 文件中，且使用中文记录。

3. **Git 提交规范**：commit 消息必须遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范且使用中文。


## 避免过度测试与过度防御

* 只处理当前需求、真实可达的失败模式，以及项目明确要求的安全边界；不要为“理论上可能发生”的情况增加防御代码。
* 不要主动增加 feature flag、兼容层、迁移框架、wrapper、checksum/hash、审计机制或其他防御性脚手架，除非当前需求确实需要。
* 运行额外测试或检查前，先判断：**它要验证哪个尚未解决的具体风险？失败结果会如何改变下一步？** 如果没有明确答案，就不要运行。
* 已有测试或真实运行已经覆盖本次修改路径时，不要再增加功能重复的 smoke test、审计或第二轮验证。
* 不要反复验证已经通过且相关代码未发生变化的内容；新的验证应由新的代码变化、失败证据或明确风险触发。
* 需要工程判断时直接判断，不要用评分表、冗长 checklist 或多轮 self-review 替代判断。
* 发现问题时优先做针对根因的最小修复，不要因为局部问题自动扩展成全面重构或防御性工程。
* 当需求已经实现、必要检查通过且没有已知阻塞问题时停止。不要仅为了“再提高一点信心”继续增加测试、抽象或防御。